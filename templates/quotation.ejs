<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cotización - Luxury Lights</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap');

        body {
            font-family: 'Inter', sans-serif;
            margin: 0;
            padding: 40px;
            color: #333;
            background-color: #fff;
            max-width: 800px;
            margin: 0 auto;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 40px;
        }

        .logo {
            font-size: 24px;
            font-weight: 700;
            line-height: 1.2;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .logo span {
            display: block;
        }

        .quotation-title {
            font-size: 24px;
            text-transform: uppercase;
            letter-spacing: 2px;
            text-align: right;
        }

        .meta-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 30px;
            font-size: 14px;
        }

        .meta-block {
            width: 48%;
        }

        .customer-info {
            margin-bottom: 40px;
            font-size: 14px;
        }

        .customer-name {
            font-weight: 700;
            margin-bottom: 5px;
        }

        .divider {
            height: 10px;
            background-color: #000;
            margin-bottom: 20px;
        }

        .project-description {
            display: flex;
            margin-bottom: 40px;
            font-size: 14px;
        }

        .project-label {
            font-weight: 700;
            width: 150px;
            flex-shrink: 0;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
            font-size: 14px;
        }

        th {
            text-align: left;
            padding: 10px 0;
            border-bottom: 1px solid #000;
            font-weight: 700;
        }

        td {
            padding: 10px 0;
            border-bottom: 1px solid #eee;
        }

        .text-right {
            text-align: right;
        }

        .zone-header {
            background-color: #f9f9f9;
            font-weight: 700;
            padding: 10px;
        }
        
        .zone-header td {
            padding-left: 10px;
        }

        .totals {
            width: 300px;
            margin-left: auto;
            font-size: 14px;
        }

        .total-row {
            display: flex;
            justify-content: space-between;
            padding: 5px 0;
        }

        .grand-total {
            background-color: #000;
            color: #fff;
            padding: 10px;
            font-weight: 700;
            margin-top: 10px;
        }

        footer {
            margin-top: 60px;
            border-top: 2px solid #000;
            padding-top: 20px;
            display: flex;
            justify-content: space-between;
            font-size: 12px;
            color: #666;
        }

        .footer-left {
            width: 60%;
        }

        .footer-right {
            text-align: right;
        }
    </style>
</head>
<body>

    <header>
        <div class="logo">
            <span>LUXURY</span>
            <span>LIGHTS</span>
            <span style="font-size: 10px; font-weight: 400; letter-spacing: 0;">ILUMINACIÓN PROFESIONAL</span>
        </div>
        <div class="quotation-title">
            COTIZACIÓN
        </div>
    </header>

    <div class="meta-info">
        <div class="meta-block">
            <strong>Cotización No:</strong> #<%= quotationNumber %><br>
            <!-- Customer ID could go here if available -->
        </div>
        <div class="meta-block text-right">
            <strong>Fecha:</strong> <%= date %><br>
            <strong>Válido hasta:</strong> <%= validUntil %>
        </div>
    </div>

    <div class="customer-info">
        <div class="customer-name"><%= customer.name %></div>
        <div><%= customer.address %></div>
        <div><%= customer.phone %></div>
    </div>

    <div class="divider"></div>

    <div class="project-description">
        <div class="project-label">Descripción del Proyecto</div>
        <div><%= projectDescription %></div>
    </div>

    <table>
        <thead>
            <tr>
                <th style="width: 50%;">Descripción</th>
                <th class="text-right" style="width: 10%;">Cant.</th>
                <th class="text-right" style="width: 20%;">Precio</th>
                <th class="text-right" style="width: 20%;">Total</th>
            </tr>
        </thead>
        <tbody>
            <% zones.forEach(function(zone) { %>
                <tr class="zone-header">
                    <td colspan="4"><%= zone.name %></td>
                </tr>
                <% zone.items.forEach(function(item) { %>
                    <tr>
                        <td><%= item.description %></td>
                        <td class="text-right"><%= item.quantity %></td>
                        <td class="text-right">₡<%= item.price.toLocaleString() %></td>
                        <td class="text-right">₡<%= item.total.toLocaleString() %></td>
                    </tr>
                <% }); %>
            <% }); %>
        </tbody>
    </table>

    <div class="totals">
        <div class="total-row">
            <span>Subtotal</span>
            <span>₡<%= totals.subtotal.toLocaleString() %></span>
        </div>
        <div class="total-row">
            <span>I.V.A. (13%)</span>
            <span>₡<%= totals.tax.toLocaleString() %></span>
        </div>
        <div class="total-row grand-total">
            <span>Total</span>
            <span>₡<%= totals.total.toLocaleString() %></span>
        </div>
    </div>

    <footer>
        <div class="footer-left">
            <p>Si tiene alguna pregunta sobre esta cotización, por favor contáctenos.</p>
            <p><strong>Luxury Lights</strong><br>info@luxurylights.com | +506 8888-8888</p>
        </div>
        <div class="footer-right">
            <p>Gracias por su preferencia!</p>
        </div>
    </footer>

</body>
</html>
